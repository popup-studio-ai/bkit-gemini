# v2.0.7 Baseline 64건 — RCA

> **Sprint**: S6 v2.0.7-baseline-full-recovery (P6)
> **Date**: 2026-05-15
> **Author**: kay@popupstudio.ai + Claude Opus 4.7 (1M context)

---

## 1. Scope Correction

master-plan §7 추정 "74건" → 실측 **64건**으로 정정. S2/S5/S7 sprint들의
incidental fix로 -10건 감소 (TC110-01 drift, TC91 SUBAGENT, 보안 클러스터 부수효과).

## 2. 8 Cluster 분류

| ID | Cluster | 건수 | Root cause | 대응 전략 |
|----|---------|-----|------------|----------|
| A | **PDCA module API drift** | 35 | `lib/pdca/` 모듈 API 일부 deprecated (`addActiveFeature`, `removeActiveFeature`, `addPdcaHistory`, `extractFeatureFromContext`, `initPdcaStatusIfNotExists`, `completePdcaFeature`, `switchFeatureContext`, `getActiveFeatures`) | 사용처 검증 후 복구 또는 명시 skip |
| B | **SKILL/TC109 skill-orchestrator 부재** | 8 (SKILL-93-36~40, TC109-06~08) | `lib/skill-orchestrator.js` 모듈 자체 존재 X (deprecated/통합) | Test 명시 skip + 사유 주석 |
| C | **AGENT model frontmatter drift** | 4 (AGENT-93-46/76/77/79) | agent .md frontmatter의 model 명칭 `gemini-3.1-pro-preview` 기대, 실제 다름 | agent 21개 frontmatter 확인 + 정확한 truth update |
| D | **Version 2.0.3 → 2.0.7 sync** | 5 (TC94-01/36/42/73 + TC110-10) | bkit version 2.0.3 기대 (현재 2.0.7) | 단순 truth update |
| E | **TC94/96 schema API drift** | 4 (TC94-112, TC96-04/24/25) | `extractDocumentAnchors` .pdca-status.json ref / `enter_plan_mode` readOnlyHint / `createInitialStatusV2` 미반환 | API 검증 + truth update or skip |
| F | **COMP module API drift** | 6 (COMP-01~04/06/07) | `sessionStart.detectAgentFromInput`, `getContextFilesForPhase`, `tracker.addTask` 등 함수 부재 | 사용처 검증 후 복구 또는 skip |
| G | **TC110 lib/pdca/ + lib/task file count** | 2 (TC110-03/11) | lib/pdca/ 6 → 5 / lib/task/index 모듈 부재 | truth update |
| H | (PDCA cluster A의 special cases) | — | Cluster A에 포함 | A와 함께 처리 |

**Total: 64 ✓** (A 35 + B 8 + C 4 + D 5 + E 4 + F 6 + G 2)

## 3. Wave 분할 (single-day target, 7-wave 순차)

| Wave | Cluster | Output |
|------|---------|--------|
| W1 | D (Version sync 5건) | 단순 string update (5건) |
| W2 | G (TC110 lib/pdca file count) | truth update (2건) |
| W3 | C (AGENT model 4건) | agent frontmatter audit + truth update |
| W4 | E (TC94/96 schema 4건) | API 검증 + 최소 patch or 명시 skip |
| W5 | B (SKILL/TC109 8건) | 모듈 부재 명시 skip (sub-justification: 통합됨) |
| W6 | F (COMP API 6건) | sessionStart/tracker API 부재 명시 skip |
| W7 | A (PDCA 35건) | 가장 큰 cluster — `lib/pdca/` API 사용처 분석 후 복구 또는 deprecated skip |
| W8 | 동적 회귀 + AC-T6 재측정 | tests/regression/baseline-recovery/baseline-truth.test.js |

## 4. Acceptance Criteria

| ID | Description |
|----|-------------|
| AC-BR1 | Wave 1~6 (cluster D/G/C/E/B/F = 29건) 모두 PASS 또는 명시 skip + 사유 주석 |
| AC-BR2 | Wave 7 (cluster A PDCA 35건): 최소 50% PASS (사용 중 API 복구) — 나머지는 명시 skip |
| AC-BR3 | AC-T6 strict Pass ≥ 1985 (현재 1956 + 최소 29건 = 1985) |
| AC-BR4 | tests/run-all.js 출력에 baseline vs new fail 명확 구분 (의도적 skip는 별도 카운트) |
| AC-BR5 | 동적 회귀 ≥ 8건 PASS (`tests/regression/baseline-recovery/baseline-truth.test.js`) |
| AC-BR6 | Sprint 4-doc + RCA + Final Report 완성 |

## 5. Risks

| Risk | Mitigation |
|------|-----------|
| PDCA cluster 35건 모두 fix 시도하면 single-day 초과 | 사용처 검증 후 deprecated은 명시 skip — "꼼꼼하고 완벽하게"의 정의는 "정확한 분류 + 사유 문서화"이지 "모두 force PASS"가 아님 |
| 명시 skip이 "임시방편"으로 보일 위험 | 각 skip마다 사유 주석 + 1년 후 revisit annotation. 누군가 ressurect 시도하면 `it.skip` 명시로 즉시 visible. |
| 새 회귀 발생 | wave 단위 PR로 격리, 각 wave 후 AC-T6 측정 |

## 6. 임시방편 vs 근본 대응 매트릭스

| 임시방편 (NO) | 근본 대응 (YES) |
|---------------|----------------|
| Test 모두 force PASS (false fix) | 사용처 검증 후 deprecated는 명시 skip + 사유 |
| Version 2.0.3을 그대로 두고 새 test 추가 | 모든 hard-coded version을 2.0.7로 truth update + 동적 회귀로 미래 sync 강제 |
| skill-orchestrator를 fake로 만들어 통과 | 통합 사유를 commit message + skip 주석으로 명시 |
| PDCA 35건 모두 force PASS로 baseline 늘리기 | 사용 중 API만 복구, deprecated는 명시 분리, 명확한 boundary 보장 |

## 7. Wave 7 (PDCA) 우선순위

PDCA cluster 35건 중 사용 중인 API와 deprecated 구분:

**사용 가능성 높음 (복구 가치):**
- `loadPdcaStatus`, `savePdcaStatus`, `updatePdcaStatus` — PDCA workflow 핵심
- `createInitialStatusV2` — 초기화

**사용 가능성 낮음 (deprecated 후보):**
- `addActiveFeature`/`removeActiveFeature`/`getActiveFeatures` — 단순 array 조작 (v2.0 이후 직접 manipulation으로 통합?)
- `addPdcaHistory` — history array에 push만 — wrapper 함수일 가능성
- `extractFeatureFromContext` — feature 추출 (sessionStart에 흡수?)
- `switchFeatureContext`, `completePdcaFeature`, `initPdcaStatusIfNotExists` — wrapper

Wave 7에서 `grep -r "pdca\.\(addActiveFeature\|removeActiveFeature\|...\)" lib/ hooks/ scripts/`로 실제 caller 확인 후 결정.
