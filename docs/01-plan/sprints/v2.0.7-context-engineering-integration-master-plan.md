# Sprint v2.0.7-context-engineering-integration — Master Plan

> **Sprint ID**: S5 | **Task ID**: #2 | **Priority**: P2
> **Date**: 2026-05-15 | **Mode**: L4 autonomous
> **Predecessor**: S3 (agent dispatch) — Sub-agent isolation 의존

## 1. Mission

Anthropic 2025-09 Context Engineering 4-strategy (Compaction / Tool-result clearing / Memory tool / Sub-agent isolation)를 bkit에 실제 통합. ENH-3 mapping doc만 있던 상태를 framework + 동적 회귀로 완성.

## 2. 4-Wave Workflow (Single-day target)

| Wave | Strategy | Files | Tests |
|------|----------|-------|-------|
| W1 | Compaction | `lib/core/compactor.js` (NEW), `pre-compress.js` (+) | `tests/regression/context-eng/compaction.test.js` 8+ |
| W2 | Tool-result clearing | `lib/core/tool-result-summarizer.js` (NEW), `after-tool.js` (+) | `tool-result-clearing.test.js` 7+ |
| W3 | Memory extraction.patch | `lib/core/agent-memory.js` (+) | `memory-extraction-patch.test.js` 8+ |
| W4 | Sub-agent isolation | `lib/gemini/agent-dispatch.js` (+) | `subagent-isolation.test.js` 6+ |

## 3. AC (8) — RCA §4 참조

## 4. Quality Gates

| Gate | S5 적용 |
|------|---------|
| M1 정합성 | extraction.patch ↔ agent-memory round-trip 양방향 동적 검증 |
| M3 보안 | BKIT_COMPACTION toggle / Memory data isolation / sub-agent context leak 0건 |
| M5 baseline | AC-T6 strict ≥ 1956 무회귀 |
| M7 docs sync | RCA + Sprint 4-doc + Final Report |
| M9 sprint scope | 4 wave framework + 25+ 동적 회귀 — 그 외 변경 X |
| M6 Performance | framework PoC. 실 token 30% 감소는 incremental sprint |
| M8 Data-Flow Integrity | sub-agent fork → result → merge round-trip 동적 검증 |

## 5. Out-of-scope (incremental)

- 실 token 30% 감소 측정 (nightly LLM session 필요)
- /pdca rollback 명령 UX
- Auto Memory inbox v0.44.0+ schema migration
