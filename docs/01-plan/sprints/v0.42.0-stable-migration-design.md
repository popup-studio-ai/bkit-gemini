# Gemini CLI v0.42.0 stable migration — Sprint Design

> **Sprint ID**: `v0.42.0-stable-migration`
> **Document Type**: Design (Architecture + Wave Implementation + Test Plan Matrix L1-L5)
> **Sprint Phase**: P3 (design) — **L2 stop after design 차단 지점**
> **Trust Level**: L2 / `stopAfter=design`
> **작성일**: 2026-05-13
> **참조 master plan**: [§4 Quality Gates](./v0.42.0-stable-migration-master-plan.md#4-quality-gates-activation-matrix-m1m10)
> **참조 PRD**: [§3 Job Stories](./v0.42.0-stable-migration-prd.md#3-job-stories-4-features--4-stories) — M1 PASS
> **참조 plan**: [§6 Implementation Order](./v0.42.0-stable-migration-plan.md#6-implementation-order-f1--f2--f3--f4) — M2 PASS
> **참조 P3 plan**: `docs/01-plan/features/gemini-cli-v0.42.0-migration.plan.md` §5 (Wave 분할) / §6 (D1~D8) / §10 (AC)
> **M3 Gate**: Testability (testable AC) → 본 design §test-plan 25 actionable AC + L1-L5 매트릭스

---

## §0. Context Anchor (Plan §0 인계)

| Key | Value |
|---|---|
| **WHY** | v0.42.0 stable 출시 + 미머지 plan 4개 적체 + PR #25827 정정 흡수 + Bx4 회귀 차단 |
| **WHO** | 사용자(kay) + sprint agents + migration-strategist agent |
| **RISK** | LOW (master plan §6.1) |
| **SUCCESS** | 10 정량 + 8 정성 KPI (PRD §2.1) |
| **SCOPE** | 코드 ~28줄 + 문서 4개 + 테스트 매트릭스 9 항목 (plan §6) |
| **OUT-OF-SCOPE** | 13건 별도 cycle 위임 + v0.43.0 시그널 다음 sprint (plan §7) |

---

## §1. Architecture Overview (Deep Codebase Analysis)

### 1.1 본 sprint의 8-stage Data Flow (M8 7-Layer 재정의)

> 본 sprint scope = **CLI/extension migration** (web app 아님). M8 dataFlowIntegrity 7-Layer를 다음 8-stage로 재정의.

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│                              본 sprint 8-stage Data Flow                              │
│                                                                                       │
│   L1 Hook (SessionStart)                                                              │
│   ┌────────────────────────────────────────────────────┐                              │
│   │ hooks/scripts/session-start.js:347-360             │                              │
│   │ BKIT_SESSION_START_VERBOSE !== 'true'              │                              │
│   │ ──> header 단일 라인 반환 (verbose body 차단)         │                              │
│   │ ──> PR #25827 워크어라운드 (v0.42.0 유지)             │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L2 Spawn (bkit-server)│                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ mcp/bkit-server.js:1123                            │                              │
│   │ spawn('gemini', args, { env, cwd, stdio })         │                              │
│   │ ──> 새 gemini binary 프로세스 별도 PID                │                              │
│   │ ──> 내부 AgentProtocol 미경유 (외부 spawn 패턴)        │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L3 CLI args 빌더       │                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ mcp/bkit-server.js:1097-1102                       │                              │
│   │ args = ['-e', agentPath, '--approval-mode',        │                              │
│   │         approvalFlag, '--tools', ...,              │                              │
│   │         '--prompt', task]                          │                              │
│   │ ──> OAuth/login args 미트리거 (R4 grep 검증)         │                              │
│   │ ──> --ignore-env 미사용 (Cx1 별도 cycle, D2)         │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L4 Extension 모드      │                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ gemini-extension.json (v2.0.7 sync)                │                              │
│   │ Gemini CLI extension 모드 로드                       │                              │
│   │ ──> agentPath/SKILL.md 읽기                          │                              │
│   │ ──> 21 agent FULL tier 카탈로그                       │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L5 Settings           │                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ .gemini/settings.json                              │                              │
│   │ ✅ experimental.gemma: false (Cx13 잠금, F1.4)      │                              │
│   │ ✅ general.topicUpdateNarration: false (F1.5)      │                              │
│   │ ✅ experimental.autoMemory: false (F1.5)           │                              │
│   │ ✅ experimental.memoryManager: false (F1.5)        │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L6 Tool result        │                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ 21 agent FULL tier 스모크                            │                              │
│   │ × 1 sample command (예: /agents refresh)            │                              │
│   │ ──> 응답 길이 > 0 + 명령 거부 0건                       │                              │
│   │ ──> R9 차단 (LLM 행동 변경 정적 분석 무력)              │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L7 Hook (return)      │                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ mcp/bkit-server.js JSON-RPC stdio 반환                │                              │
│   │ structured logging NDJSON 무결성                       │                              │
│   │ ──> PR #26264 informational logs JSON 누설 차단        │                              │
│   └─────────────────────┬──────────────────────────────┘                              │
│                         │                                                              │
│   L8 Output             │                                                              │
│   ┌─────────────────────▼──────────────────────────────┐                              │
│   │ tests/run-all.js baseline                          │                              │
│   │ ──> pass >= 1925/2032 (94.7%)                       │                              │
│   │ ──> tc115 8/8 PASS                                  │                              │
│   │ ──> tc113 PASS (PR #25827 카나리아)                   │                              │
│   │ ──> 카나리아 시나리오 A/B1/B2/C 4/4 PASS               │                              │
│   └────────────────────────────────────────────────────┘                              │
│                                                                                       │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 8-stage × M8 s1Score 매트릭스

| Layer | 단계 | 검증 항목 | 통과 기준 | s1Score 기여 (12.5점) |
|---|---|---|---|---|
| L1 | Hook (SessionStart) | `hooks/scripts/session-start.js` BKIT_SESSION_START_VERBOSE slim default | tc113/tc114 PASS | 12.5 |
| L2 | Spawn (bkit-server) | `mcp/bkit-server.js:1123` spawn 정상 | 21 agent 1/21 spot PASS | 12.5 |
| L3 | CLI args 빌더 | `mcp/bkit-server.js:1097-1102` args 무변경 grep | OAuth/login args 0건 | 12.5 |
| L4 | Extension 모드 | `gemini-extension.json` v2.0.7 sync + 로드 | `npx --yes @google/gemini-cli@0.42.0 -e <agentPath>` PASS | 12.5 |
| L5 | Settings | `.gemini/settings.json` 4 잠금 | jq 검증 모두 `false` | 12.5 |
| L6 | Tool result | 21 agent FULL tier smoke | 21/21 PASS (또는 fallback 5/5) | 12.5 |
| L7 | Hook (return) | NDJSON 무결성 | structured logging valid | 12.5 |
| L8 | Output | baseline + 카나리아 | pass >= 1925 + 카나리아 4/4 | 12.5 |

**s1Score 만점**: 8 × 12.5 = **100점**. 1 stage fail 시 87.5점. **Pass 기준: s1Score == 100**.

### 1.3 본 sprint 코드베이스 핵심 모듈 매트릭스

| 모듈 | 경로 | 변경 라인 | Wave | 검증 |
|---|---|---|---|---|
| **bkit.config.json** | (루트) line 120 (`testedVersions`) | +6 | Wave 1 (F1.1) | F1.7 grep |
| **package.json** | (루트) `@google/gemini-cli` | +1 -1 | Wave 1 (F1.2) | F1.8 npm install |
| **lib/gemini/version.js** | line 201-211 (`isVersionAtLeast` + flags) | +9 (capability flags) | Wave 1 (F1.3) | tc04 unit (F2.6 baseline 일부) |
| **.gemini/settings.json** | (루트) | +4 (jq 검증 가능 키 4개) | Wave 1 (F1.4, F1.5) | F2.13 jq 검증 |
| **tests/suites/tc38-feature-flags-matrix.js** | (`tc38`) | +9 (매트릭스 항목) | Wave 1 (F1.6) | tc38 unit (F2.6 baseline 일부) |
| **mcp/bkit-server.js** | line 1097-1102 (args 빌더), line 1119 (`GEMINI_CLI_TRUST_WORKSPACE=true`), line 1123 (spawn) | 0 (변경 없음, grep만) | Wave 2 (F2.4) | grep |
| **hooks/scripts/session-start.js** | line 347-360 (BKIT_SESSION_START_VERBOSE) | 0 (변경 없음) | Wave 2 (F2.14) | F2.14 grep |
| **skills/gemini-cli-learning/SKILL.md** | 12 단락 추가 (v0.40.0 5 + v0.41.x 6 + v0.42.0 stable 1) | +~60 | Wave 3 (F3.3) | grep |
| **GEMINI.md** | line 1, 67 (version) + v0.42.0 단락 | +5 -2 | Wave 3 (F3.1) | grep |
| **README.md** | 뱃지 + testedVersions | +3 -2 | Wave 3 (F3.2) | grep |
| **bkit.config.json** | version 필드 | +1 -1 | Wave 3 (F3.4) | grep |
| **gemini-extension.json** | version 필드 | +1 -1 | Wave 3 (F3.5) | grep |
| **docs/01-plan/features/v2.1.0-context-optimization.plan.md** | trigger memo 1단락 | +5 | Wave 3 (F3.6) | grep |

**Total**: 13 modules × ~28 lines (코드) + ~60 lines (docs) = **~88 lines net change**.

### 1.4 의존성 그래프 (모듈 간)

```
                           bkit.config.json (testedVersions)
                                      │
                                      ▼
                           lib/gemini/version.js (flags)
                                      │
                                      ▼
                          tests/suites/tc38-...js (매트릭스)
                                      │
                                      ▼
                            .gemini/settings.json (잠금)
                                      │
                                      ▼
              ┌───────────────────────┼───────────────────────┐
              │                       │                       │
              ▼                       ▼                       ▼
   mcp/bkit-server.js          hooks/scripts/           tests/run-all.js
   (args 빌더 grep)             session-start.js         (baseline)
              │                  (PR #25827 grep)              │
              ▼                       │                       ▼
         21 agent smoke               │                  tc115/tc113
              │                       │                  카나리아 A/B1/B2/C
              ▼                       │                       │
         ─────────────────────────────┴─────────────────────────
              │
              ▼
  skills/gemini-cli-learning/SKILL.md (12 단락)
              │
              ▼
   GEMINI.md / README.md / bkit.config.json / gemini-extension.json (v2.0.7 sync)
              │
              ▼
   docs/01-plan/features/v2.1.0-context-optimization.plan.md (trigger memo)
              │
              ▼
      branch + commit + PR + 사용자 결재 → archived
```

---

## §2. Wave Implementation Design (P3 plan §5 인계)

### 2.1 Wave 1 (P0, F1, ~50분) — Critical Patch + Cx13 잠금

#### 2.1.1 F1.1: testedVersions 6개 활성화 (2분)

**파일**: `bkit.config.json:120`

**변경 전**:
```json
"testedVersions": ["0.34.0", "0.35.0", ..., "0.39.1"]
```

**변경 후**:
```json
"testedVersions": [
  "0.34.0", "0.35.0", "0.36.0", "0.37.0", "0.38.0", "0.38.1", "0.38.2", "0.39.0", "0.39.1",
  "0.40.0", "0.40.1", "0.41.0", "0.41.1", "0.41.2", "0.42.0"
]
```

**의도**: v0.40.0/v0.40.1/v0.41.0/v0.41.1/v0.41.2/v0.42.0 6개 누적 등록. N=15 (D7 N=13 trigger 다음 sprint 재검토 대상).

**검증**: `node -e "JSON.parse(require('fs').readFileSync('bkit.config.json', 'utf8'))"` PASS + `grep -c '"0\.42\.0"' bkit.config.json` == 1.

#### 2.1.2 F1.2: package.json dep bump v0.39.1 → v0.42.0 (1분 + 3분 install)

**파일**: `package.json`

**변경 전**:
```json
"dependencies": {
  "@google/gemini-cli": "^0.39.1"
}
```

**변경 후**:
```json
"dependencies": {
  "@google/gemini-cli": "^0.42.0"
}
```

**의도**: npm `latest` 채널 = v0.42.0 매핑. `package-lock.json` 자동 갱신 (npm install).

**검증**: `npm install` 정상 종료 + `cat package-lock.json | grep -A 2 "@google/gemini-cli"` v0.42.0 반영.

#### 2.1.3 F1.3: lib/gemini/version.js 9 flag 신설 (15분)

**파일**: `lib/gemini/version.js:201-211` 뒤

**변경 후 (추가 라인)**:
```javascript
// v0.40.0+ flags (4)
const hasContinueOnFailedApiCallRemoved = isVersionAtLeast('0.40.0'); // PR #26340
const hasNewToolDisplay = isVersionAtLeast('0.40.0'); // PR #25186
const hasExitPlanModeShellBan = isVersionAtLeast('0.40.0'); // PR #26230
const hasSetSessionIdReset = isVersionAtLeast('0.40.0'); // PR #26342

// v0.41.0+ flags (4)
const hasA2aServerSplit = isVersionAtLeast('0.41.0'); // (참조)
const hasAutoMemoryInbox = isVersionAtLeast('0.41.0'); // Cx2 base
const hasIgnoreEnvFlag = isVersionAtLeast('0.41.0'); // Cx1 base
const hasPromptUndeprecated = isVersionAtLeast('0.41.0'); // Cx14

// v0.42.0+ flag (1)
const hasGemmaDefaultOn = isVersionAtLeast('0.42.0'); // PR #26307 — Bx4 잠금 트리거
```

**의도**: Wave 2 spot 검증 + Wave 3 SKILL 단락에서 capability flag 활용. `hasGemmaDefaultOn`이 본 cycle 핵심 (Cx13 잠금 게이트).

**검증**: tc04 unit (F2.6 baseline 일부) + `grep -c "hasGemmaDefaultOn" lib/gemini/version.js` == 1.

#### 2.1.4 F1.4: Cx13 잠금 (5분)

**파일**: `.gemini/settings.json`

**변경 전** (관련 부분):
```json
{
  "experimental": {
    "autoMemory": false
  }
}
```

**변경 후**:
```json
{
  "experimental": {
    "autoMemory": false,
    "memoryManager": false,
    "gemma": false
  },
  "general": {
    "topicUpdateNarration": false
  }
}
```

**의도** (Cx13 / D3 결재):
- `experimental.gemma: false` — Bx4 default-on (`false` → `true`) 회귀 사전 차단. No Guessing 강화 (사용자가 명시 모델 선택 권장).
- `experimental.memoryManager: false` — v0.41.x 신설 옵션 명시 잠금 (v2.1.0까지 미사용).
- `general.topicUpdateNarration: false` — topic narration 명시 잠금 (preview plan W1.3).

**검증**: `cat .gemini/settings.json | jq '.experimental.gemma'` == `false` (F2.13).

#### 2.1.5 F1.5: settings.json 명시 잠금 (10분)

**파일**: `.gemini/settings.json` (F1.4와 동일 파일, autoMemory/memoryManager/topicUpdateNarration 명시)

**의도**: P3 plan W1.3, W1.4 그대로. autoMemory inbox (Cx2)는 v2.1.0 PoC까지 미활용, memoryManager는 v0.41.x 신설 옵션 미활용.

**검증**: `cat .gemini/settings.json | jq '.experimental | keys'` = `["autoMemory", "gemma", "memoryManager"]` + `cat .gemini/settings.json | jq '.general.topicUpdateNarration'` == `false`.

#### 2.1.6 F1.6: tc38 매트릭스 갱신 (8분)

**파일**: `tests/suites/tc38-feature-flags-matrix.js`

**변경**: 9개 capability flag (F1.3 신설 분) × 검증 entry 추가. 기존 매트릭스 패턴 그대로 (P3 plan W1.5).

**검증**: `node tests/run-all.js --suite=tc38` PASS.

#### 2.1.7 F1.7: testedVersions baseline grep 0건 재확인 (5분)

**명령**: `grep -rn "0\.39\.1" tests/ bkit.config.json | grep -v testedVersions`

**의도**: 기존 v0.39.1 hardcode가 testedVersions 외 위치에 잔존하지 않는지 확인. 회귀 0건 보장.

**검증**: 결과 0건 (또는 문서/주석만).

#### 2.1.8 F1.8: npm install + lockfile 검증

**명령**:
```bash
npm install
cat package-lock.json | grep '"@google/gemini-cli"' -A 2
```

**검증**: lockfile에 v0.42.0 반영 + `node_modules/@google/gemini-cli/package.json`의 version 필드 == `0.42.0`.

### 2.2 Wave 2 (P1, F2, ~3-3.5h) — Smoke + Baseline + 카나리아

#### 2.2.1 F2.1: tc113/tc107 spot 검증 (15분)

**명령**: `find tests/suites -name "tc107*" -o -name "tc113*" -o -name "*25655*"`

**의도**: PR #25827 워크어라운드 카나리아(tc113) + tc107 (참조)의 존재 확인. 부재 시 P1 부채 등록.

**검증**: tc113-session-start-duplication-defense.js 파일 존재.

#### 2.2.2 F2.2: topic narration L3 baseline (10분)

**명령**:
```bash
cd /tmp && npx --yes @google/gemini-cli@0.42.0 -p "list 3 numbers" 2>&1 | tee narration-baseline.log
grep -ic "topic" narration-baseline.log
```

**의도**: F1.5의 `general.topicUpdateNarration: false` 잠금이 실제 실행에 반영되는지 L3 실측. narration 줄 == 0.

**검증**: grep 결과 0.

#### 2.2.3 F2.3: tools.core 키 schema spot (10분)

**명령**: `npx --yes @google/gemini-cli@0.42.0 --help 2>&1 | grep -i "tools\.core\|coreTools"`

**의도**: `tools.core` vs `tools.coreTools` 키 schema 확정 (P3 plan Q3).

**검증**: 한 가지 명칭만 출력.

#### 2.2.4 F2.4: bkit-permissions deny 우선순위 spot (15분)

**명령**: sandbox 시뮬 (P3 plan W2.4 그대로).

**의도**: bkit-permissions deny 정책이 v0.42.0에서 우선순위 깨지지 않는지 확인.

**검증**: deny > allow 우선순위 유지.

#### 2.2.5 F2.5: **21 agent FULL tier 스모크 (60분)** — R9 차단

**명령**: 21개 agent × 1개 표준 명령 (예: `/agents refresh` 또는 자기 진단 query) × 응답 정상 확인.

**의도**: P3 plan §8.2 절차 그대로. LLM 행동 변경은 정적 분석 무력 → 21 agent 1개씩 동적 검증으로 회귀 검출.

**검증**: 21/21 PASS (응답 길이 > 0 + 명령 거부 0건).

**Fallback** (BUDGET_EXCEEDED 시 시나리오 4): 5개 핵심 agent (sprint-master-planner / sprint-orchestrator / sprint-qa-flow / migration-strategist / gemini-researcher) × 5/5 PASS.

#### 2.2.6 F2.6: full baseline (30-60분)

**명령**: `node tests/run-all.js 2>&1 | tee baseline-v0.42.0.log`

**의도**: v0.39.1 baseline (1925/2032 = 94.7%) 회복 확인. tc04 / tc38 / tc115 / tc113 등 모든 suite 포함.

**검증**: `grep "Total:" baseline-v0.42.0.log` pass >= 1925.

#### 2.2.7 F2.7: 카나리아 시나리오 A/B1/B2/C 4건 PASS (30분)

**명령**: F2.6 결과 grep + 시나리오별 명시 검증.

**시나리오**:
- **A**: 기본 spawn + 21 agent FULL tier 동작
- **B1**: PR #25827 워크어라운드 활성 (tc113 PASS)
- **B2**: tc115 v0.39.1 headless trust 8 tests
- **C**: bootstrap-trust.sh idempotent 2/2

**검증**: 4/4 PASS.

#### 2.2.8 F2.8: a2a-server grep 0건 재확인 (5분)

**명령**: `grep -rn "a2a-server" mcp/ lib/ hooks/ tests/` + `cat package.json | grep a2a`

**의도**: v0.41.2 cycle PR #26589 흡수 (a2a-server 분리) 보존 확인.

**검증**: docs 외 0건.

#### 2.2.9 F2.9: tc115 v0.39.1 회귀 (10분)

**명령**: `node tests/run-all.js --suite=tc115`

**의도**: v0.39.1 headless trust enforcement 워크어라운드(`GEMINI_CLI_TRUST_WORKSPACE=true`, commit e00c067) 보존 확인.

**검증**: 8/8 PASS.

#### 2.2.10 F2.10: bootstrap-trust.sh idempotent (5분)

**명령**: `bash scripts/bootstrap-trust.sh` × 2회.

**의도**: idempotent 동작 보존.

**검증**: 2/2 PASS (두 번째 실행 시 변경 없음 + exit 0).

#### 2.2.11 F2.11: SKILL placeholder (5분) — 본 cycle delta 1 Low

**파일**: `skills/gemini-cli-learning/SKILL.md`

**변경**: v0.42.0 stable placeholder 1줄 추가 (Wave 3 F3.3에서 풀 단락으로 확장).

**의도**: P2 §3.1 잔존 1 Low (`skills/gemini-cli-learning/SKILL.md` v0.42.0 stable placeholder) 처리.

**검증**: `grep -c "v0\.42\.0" skills/gemini-cli-learning/SKILL.md` >= 1.

#### 2.2.12 F2.12: Bx0~Bx4 정적 분석 재현 (5분)

**명령**:
```bash
grep -rn "continueOnFailedApiCall\|InvalidStream\|exit_plan_mode\|Config\.setSessionId" lib/ mcp/ hooks/
```

**의도**: P2 §1.2~§1.5의 Bx0~Bx4 영향 0건 재확인.

**검증**: docs/test 외 0건.

#### 2.2.13 F2.13: Cx13 잠금 검증 (3분)

**명령**: `cat .gemini/settings.json | jq '.experimental.gemma'`

**의도**: F1.4의 Cx13 잠금이 실제로 적용됐는지 확인.

**검증**: `false`.

#### 2.2.14 F2.14: PR #25827 워크어라운드 9 위치 grep (5분)

**명령**:
```bash
grep -rn "BKIT_SESSION_START_VERBOSE\|tc113-session-start-duplication" hooks/ tests/ GEMINI.md
```

**의도**: P2 §2.2 9 위치 모두 유지 확인 (D5 명문화 전제 조건).

**검증**: 9 위치 그대로 유지 (`hooks/scripts/session-start.js:347-360` + tc113 + tc114 + tc01/08/10/22 + `GEMINI.md:30`).

### 2.3 Wave 3 (P2, F3, ~1h) — 문서 + SKILL 풀 단락

#### 2.3.1 F3.1: GEMINI.md v2.0.6 → v2.0.7 + 변경사항 단락 (10분)

**파일**: `GEMINI.md:1, 67`

**변경**:
- 헤더: `bkit-gemini v2.0.6` → `bkit-gemini v2.0.7`
- footer / 변경사항 단락: v0.42.0 stable 통합 흡수 명시 + Cx13 잠금 + PR #25827 정정 흡수

#### 2.3.2 F3.2: README.md 뱃지 + testedVersions (10분)

**파일**: `README.md`

**변경**:
- 뱃지: `Gemini CLI v0.39.1` → `Gemini CLI v0.42.0`
- testedVersions 6개 추가 명시
- 신규 안내 1단락 (v2.0.7 머지 변경사항 요약)

#### 2.3.3 F3.3: SKILL.md 12 단락 (30분) — **D5 정정 명문화**

**파일**: `skills/gemini-cli-learning/SKILL.md`

**변경**: 12 단락 추가

| 단락 | 버전 | 내용 |
|---|---|---|
| 단락 1~2 | v0.40.0 / v0.40.1 | preview plan §1~§7 인용 (Bx0~Bx4 + Cx*) |
| 단락 3~5 | v0.41.0 / v0.41.1 / v0.41.2 | preview plan §1~§7 인용 (`memoryManager` 등) |
| 단락 6 | v0.41.2 a2a-server 분리 | a2a-server grep 0건 확인 보존 |
| 단락 7~11 | v0.42.0-preview.0/.1/.2 | preview plan (Cx13 + Cx14 등) |
| **단락 12** | **v0.42.0 stable** | **"v0.42.0 stable (2026-05-12 출시): preview.2 bit-for-bit promotion (code 0건). Bx4 Gemma 4 default-on → `.gemini/settings.json` `experimental.gemma: false` 잠금. Cx13 `hasGemmaDefaultOn` flag 신설. Cx1~Cx14 14건 중 Cx13만 채택, 나머지 13건 별도 cycle 위임. PR #25827 정정: main MERGED 2026-05-11이나 v0.42.0 release 브랜치 미포함 → SessionStart `systemMessage` 워크어라운드(`BKIT_SESSION_START_VERBOSE` slim default + tc113/tc114 + 7개 tc 환경변수 명시) v0.42.0 cycle 유지, v0.43.0 stable cycle 제거 후보. v0.43.0-preview.0 동시 출시 시그널 §11 참조"** |

#### 2.3.4 F3.4 / F3.5: bkit.config.json + gemini-extension.json version sync (2분)

**파일**: `bkit.config.json` `version` 필드 + `gemini-extension.json` `version` 필드

**변경**: `"2.0.6"` → `"2.0.7"` (두 파일 모두).

#### 2.3.5 F3.6: v2.1.0 trigger memo (5분)

**파일**: `docs/01-plan/features/v2.1.0-context-optimization.plan.md`

**변경**: 1단락 추가 — v0.42.0 stable 마이그레이션 완료 + v0.43.0-preview.0 시그너 (Subagent Protocol / Session persistence / Context 강화) v2.1.0 시너지 1줄.

#### 2.3.6 F3.7: D8 v0.43.0-preview.0 §11 시그널 단락 (5분)

**파일**: sprint master plan §7.4 (carry items 9 항목) 이미 명문화. design 본문 §3 참조 명시.

**검증**: master plan §7.4의 9 carry items가 다음 sprint 인계 명세 (R15 / D8 결재 자동).

### 2.4 Wave 4 (PR consolidation, F4, ~30분)

#### 2.4.1 F4.1: branch cut (2분)

```bash
git checkout -b feature/v2.0.7-gemini-cli-v0.42.0-stable-migration
```

#### 2.4.2 F4.2: 통합 커밋 (10분)

**Commit message**:
```
feat(v2.0.7): Gemini CLI v0.39.1 → v0.42.0 cumulative migration (B' 13회차)

Strategy B' 13회차 (가중 8.85점), 5 cycle 단일 PR 통합 흡수 (D1 시나리오 A):
- v0.40.0 / v0.41.1 / v0.41.2 / v0.42.0-preview / v0.42.0 stable

Wave 1 (P0):
- bkit.config.json: testedVersions 6개 추가 (0.40.0/0.40.1/0.41.0/0.41.1/0.41.2/0.42.0)
- package.json: @google/gemini-cli ^0.42.0 (was ^0.39.1)
- lib/gemini/version.js: 9 capability flags (hasGemmaDefaultOn 포함)
- .gemini/settings.json: Cx13 잠금 (experimental.gemma: false) + autoMemory/memoryManager/topicUpdateNarration 명시
- tests/suites/tc38: 9 항목 추가

Wave 2 (P1):
- 21 agent FULL tier 스모크 21/21 PASS (R9 차단)
- baseline 1925/2032 (94.7%) 회복
- 카나리아 A/B1/B2/C 4/4 PASS
- a2a-server grep 0건 + Bx0~Bx4 정적 분석 재현
- PR #25827 워크어라운드 9 위치 grep 재확인

Wave 3 (P2):
- bkit v2.0.7 일관성 (GEMINI.md / README.md / bkit.config.json / gemini-extension.json)
- skills/gemini-cli-learning/SKILL.md 12 단락 (v0.42.0 stable 풀 단락 = PR #25827 정정 명문화)

Decisions:
- D1: 시나리오 A 활성화 (stable 출시 후 5 cycle 통합 단일 PR)
- D3: Cx13 본 cycle 채택 (5분 + flag 5분 = 10분)
- D4: PR #25827 워크어라운드 v0.42.0 cycle 유지
- D5: PR #25827 정정 흡수 명문화 (No Guessing 강화)
- D8: v0.43.0-preview.0 시그널 §11 풀 등록 (carry item)

Risks: LOW (R12/R14 소진, R15/R16 NEAR-ZERO, R4 bkit scope 외)

Supersedes:
- docs/01-plan/features/gemini-cli-v0.40.0-migration.plan.md
- docs/01-plan/features/gemini-cli-v0.41.1-migration.plan.md
- docs/01-plan/features/gemini-cli-v0.41.2-migration.plan.md
- docs/01-plan/features/gemini-cli-v0.42.0-preview-migration.plan.md

Next sprint: v0.43.0-stable-migration (carry items 9건 인계)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

#### 2.4.3 F4.3: PR 작성 (15분)

```bash
gh pr create --title "feat(v2.0.7): Gemini CLI v0.39.1 → v0.42.0 cumulative migration (B' 13회차)" --body "$(cat <<'EOF'
## Sprint Summary

- Sprint ID: `v0.42.0-stable-migration`
- Strategy: B' 13회차 (가중 8.85점)
- 5 cycle 통합 흡수 (D1 시나리오 A 활성화)
- LOW 위험도, ~3.5-4.5h + 30분 Buffer
- YAGNI 92.8% (Cx13만 채택, 13건 별도 cycle 위임)

## D1~D8 Decisions

- ✅ D1: 시나리오 A 활성화
- ✅ D3: Cx13 본 cycle 채택
- ✅ D4: PR #25827 워크어라운드 v0.42.0 유지
- ✅ D5: PR #25827 정정 흡수 명문화
- ⏳ D7: testedVersions N=13 trigger (다음 sprint 인계)
- ⏳ D8: v0.43.0-preview.0 §11 풀 등록 (다음 sprint 인계)

## Test Plan

- [x] L1 unit: tc04 / tc38 / smoke PASS
- [x] L2 baseline: pass >= 1925/2032 (94.7%)
- [x] L3 spot: topic narration / tools.core / bkit-permissions
- [x] L4 21 agent FULL tier 21/21 PASS (R9 차단)
- [x] L5 카나리아 A/B1/B2/C 4/4 PASS
- [x] tc115 v0.39.1 회귀 8/8 PASS
- [x] tc113 카나리아 PASS
- [x] a2a-server grep 0건
- [x] Bx0~Bx4 정적 분석 재현 grep 0건
- [x] Cx13 잠금 검증 (experimental.gemma == false)
- [x] PR #25827 워크어라운드 9 위치 유지 grep

## Carry Items (다음 sprint v0.43.0-stable-migration)

1. Subagent Protocol (#25302/#25303) 호환성 검증
2. Session persistence (#26514) PoC
3. Context 강화 (#26888/#26655) 자동 흡수
4. PR #25827 워크어라운드 9 위치 → 1 위치 제거
5. OAuth headless fix (#26571) 자동 흡수
6. Chat corruption fix (#26534) 자동 흡수
7. Async hysteresis fix (#26452) 자동 흡수
8. testedVersions 정책 재검토 (D7, N=13)
9. Auto Memory (Cx2) PoC → v2.1.0 cycle

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

#### 2.4.4 F4.4: 사용자 결재 대기 (L2 stop point)

**조건**: autorunScope=design 차단 지점에서 Do 진입 전 결재.

**결재 항목** (4건):
- D3: Cx13 채택
- D4: PR #25827 워크어라운드 v0.42.0 유지
- D5: PR #25827 정정 흡수 명문화
- (D1 자동, D6 자동, D7/D8 carry item)

**ACK 후 행동**: `gh pr merge` → sprint state `archived` → audit log `sprint_archived`.

---

## §3. Test Plan Matrix L1-L5 (M3 Gate 핵심)

### 3.1 L1 (Unit) — Wave 1 자동

| Test | 명령 | Pass 기준 | Feature |
|---|---|---|---|
| `tc04` | `node tests/run-all.js --suite=tc04` | PASS | F1.3 (version flags) |
| `tc38` | `node tests/run-all.js --suite=tc38` | PASS | F1.6 (feature flags matrix) |
| `smoke` | `node tests/run-all.js --suite=smoke` | PASS | F1.1 + F1.2 (testedVersions + dependency) |
| json schema valid | `node -e "JSON.parse(...)"` | PASS | F1.1 + F1.4 + F1.5 + F3.4 + F3.5 |
| npm install | `npm install` | PASS (exit 0) | F1.2 |
| package-lock 검증 | `cat package-lock.json | grep "@google/gemini-cli"` | v0.42.0 반영 | F1.8 |

### 3.2 L2 (Integration / Baseline) — Wave 2 자동

| Test | 명령 | Pass 기준 | Feature |
|---|---|---|---|
| full baseline | `node tests/run-all.js` | pass >= 1925/2032 (94.7%) | F2.6 |
| tc115 v0.39.1 회귀 | `node tests/run-all.js --suite=tc115` | 8/8 PASS | F2.9 |
| tc113 카나리아 | `node tests/run-all.js --suite=tc113` | PASS | F2.1 + F2.7 (B1) |
| bootstrap-trust idempotent | `bash scripts/bootstrap-trust.sh` × 2 | 2/2 PASS | F2.10 |
| bkit-permissions deny | sandbox 시뮬 | deny > allow 유지 | F2.4 |

### 3.3 L3 (Spot / End-to-end CLI) — Wave 2 수동/반자동

| Test | 명령 | Pass 기준 | Feature |
|---|---|---|---|
| topic narration L3 | `cd /tmp && npx --yes @google/gemini-cli@0.42.0 -p "list 3 numbers"` × 1회 | narration 줄 == 0 | F2.2 |
| tools.core schema | `npx --yes @google/gemini-cli@0.42.0 --help` | `tools.core` 명칭 확정 | F2.3 |

### 3.4 L4 (21 Agent FULL tier Smoke) — Wave 2, R9 차단

| Test | 명령 | Pass 기준 | Feature |
|---|---|---|---|
| 21 agent FULL tier | 21개 agent × 1 sample command | 21/21 PASS (응답 길이 > 0 + 명령 거부 0건) | F2.5 |
| Fallback (시나리오 4 대응) | 5 핵심 agent | 5/5 PASS | F2.5 fallback |

### 3.5 L5 (카나리아 + grep + static) — Wave 2 자동

| Test | 명령 | Pass 기준 | Feature |
|---|---|---|---|
| 카나리아 A (기본 spawn + 21 agent) | F2.6 + F2.5 결과 grep | PASS | F2.7 A |
| 카나리아 B1 (PR #25827 워크어라운드 활성) | tc113 결과 grep | PASS | F2.7 B1 |
| 카나리아 B2 (tc115 v0.39.1) | tc115 결과 grep | PASS | F2.7 B2 |
| 카나리아 C (bootstrap-trust idempotent) | bash scripts/bootstrap-trust.sh × 2 결과 grep | PASS | F2.7 C |
| a2a-server grep | `grep -rn "a2a-server" mcp/ lib/ hooks/ tests/` | docs 외 0건 | F2.8 |
| Bx0~Bx4 정적 분석 재현 | `grep -rn "continueOnFailedApiCall\|..." lib/ mcp/ hooks/` | docs/test 외 0건 | F2.12 |
| Cx13 잠금 검증 | `cat .gemini/settings.json | jq '.experimental.gemma'` | `false` | F2.13 |
| PR #25827 워크어라운드 9 위치 | `grep -rn "BKIT_SESSION_START_VERBOSE\|..." hooks/ tests/ GEMINI.md` | 9 위치 유지 | F2.14 |
| OAuth args 빌더 미트리거 (R4) | `grep -rn "oauth\|login" mcp/bkit-server.js` | OAuth args 0건 | M7 |

### 3.6 25 Actionable AC 요약 (M3 PASS 기준)

| Wave | AC 수 | 매핑 |
|---|---|---|
| Wave 1 (F1) | **8** | AC-F1-1 ~ AC-F1-8 (PRD §3.1) |
| Wave 2 (F2) | **14** | AC-F2-1 ~ AC-F2-14 (PRD §3.2) |
| Wave 3 (F3) | **7** | AC-F3-1 ~ AC-F3-7 (PRD §3.3) |
| Wave 4 (F4) | **4** | AC-F4-1 ~ AC-F4-4 (PRD §3.4) |
| **Total** | **33** | 모두 testable + grep/jq/test-suite 검증 가능 |

> M3 Gate 기준: 25 AC 이상 → **본 sprint 33 AC = 132% 충족**. M3 PASS.

---

## §4. M Gate × Phase 활성화 매트릭스 (master plan §4 인계)

| Phase | Gate | 활성 시점 | Pass 기준 |
|---|---|---|---|
| prd | M1 | PRD §1 작성 완료 | 5W1H ✅ |
| plan | M2 | Plan §2 + §3 작성 완료 | 5 strategies + D1~D8 ✅ |
| **design (본 phase)** | **M3** | **design §test-plan + 25 AC 명시** | **33 AC (132% 충족) ✅** |
| do | M4 | F1 + F2 spot + F4.1~F4.2 완료 | 28줄 코드 + dependency bump + tc38 9 항목 |
| do | M6 | code-analyzer 회귀 0건 | 회귀 0건 |
| iterate | M5 | matchRate >= 90 | 1925/2032 baseline 회복 |
| qa | M7 | R4 grep 검증 | OAuth args 0건 |
| qa | M8 | 7-Layer s1Score == 100 | 8-stage 모두 통과 |
| report | M9 | 9 KPI PASS | 위 KPI 매트릭스 모두 충족 |
| archive | M10 | carry items 9건 | master plan §7.4 명문화 |

---

## §5. Codebase Analysis: 주요 의존성 검증

### 5.1 bkit-server (`mcp/bkit-server.js`) 외부 spawn 패턴 (R15 무력화 근거)

```javascript
// mcp/bkit-server.js:1097-1102 (args 빌더)
const args = [
  '-e', agentPath,           // extension 모드 (Gemini CLI를 도구로 외부 호출)
  '--approval-mode', approvalFlag,
  '--tools', toolIsolation,
  '--prompt', task,
];

// mcp/bkit-server.js:1119 (commit e00c067, v0.39.1 cycle)
env.GEMINI_CLI_TRUST_WORKSPACE = 'true';  // Headless Trust Enforcement bypass (PR #25814)

// mcp/bkit-server.js:1123 (spawn 호출)
spawn('gemini', args, { env, cwd, stdio });
```

**의의** (R15 무력화):
- bkit는 매 subagent 호출마다 *새 gemini binary 프로세스를 별도 PID로 spawn*
- 내부 `AgentProtocol` 미경유 (외부 spawn 패턴)
- v0.43.0의 Subagent Protocol 추상화 (Dx4 + Dx5)는 *내부 API* 변경 → bkit 영향 0건
- `invoke_agent` 도구 미호출 → v0.43.0 stable cycle에서 `lib/gemini/policy.js` `generateSubagentRules()` 회귀 스모크 ~10분만 필요

### 5.2 SessionStart 워크어라운드 (`hooks/scripts/session-start.js:347-360`) — D5 명문화 대상

```javascript
// hooks/scripts/session-start.js:347-360
function generateDynamicContext() {
  if (process.env.BKIT_SESSION_START_VERBOSE !== 'true') {
    // header 단일 라인만 반환 (verbose body 차단)
    // PR #25827 fix v0.42.0 release 브랜치 미포함 → 워크어라운드 유지
    return '## bkit-gemini (slim mode — set BKIT_SESSION_START_VERBOSE=true for full)\n';
  }
  // verbose body 생성
  return generateFullContext();
}
```

**관련 9 위치** (F2.14 grep 대상):
1. `hooks/scripts/session-start.js:347-360` — 코드 워크어라운드
2. `tests/suites/tc113-session-start-duplication-defense.js` — 카나리아
3. `tests/suites/tc114-session-start-slim-mode.js` — slim mode 검증
4. `tests/suites/tc01-hooks.js:73, 83` — 환경변수 명시
5. `tests/suites/tc08-context.js:28` — 환경변수 명시
6. `tests/suites/tc10-philosophy.js:13` — 환경변수 명시
7. `tests/suites/tc22-pdca-status-path.js:16, 28` — 환경변수 명시
8. `GEMINI.md:30` — 사용자 verbose 모드 안내
9. (`docs/02-design/features/bkit-v2.0.5-finalization.design.md:39~81` + `docs/04-report/bkit-v2.0.5-finalization.report.md:18~197` — historical, 변경 불필요)

### 5.3 lib/gemini/version.js capability flag 패턴 (F1.3 모델)

```javascript
// lib/gemini/version.js:163 (예시 — 기존 패턴)
const hasSubagentPolicies = isVersionAtLeast('0.34.0');
const hasInvokeAgent = isVersionAtLeast('0.34.0');  // legacy SubagentTool replaced (PR #25053 → PR #24489)

// F1.3에서 추가될 v0.42.0+ flag
const hasGemmaDefaultOn = isVersionAtLeast('0.42.0');  // PR #26307 — Bx4 잠금 트리거
```

**의의**: 새 capability flag는 *읽기 전용*. bkit는 외부 spawn 패턴이므로 flag는 SKILL 단락 작성 + tc38 매트릭스 + 향후 cycle 분기에만 사용.

---

## §6. Pre-flight Checks (do phase 진입 전 자동 검증)

### 6.1 사전 조건 (design → do 전이)

- [ ] M3 PASS: 33 actionable AC (132% 충족)
- [ ] D3/D4/D5 사용자 결재 (L2 stop point)
- [ ] git status clean (uncommitted 변경 0건)
- [ ] `git log -1 --oneline` = `8e0daa7` (main HEAD)
- [ ] sprint state JSON `.bkit/state/sprints/v0.42.0-stable-migration.json` `currentPhase == "design"` AND `phases.design == "complete"`

### 6.2 do 진입 시 행동 (sprint-orchestrator agent)

1. `git checkout -b feature/v2.0.7-gemini-cli-v0.42.0-stable-migration`
2. F1.1 → F1.2 → F1.3 → F1.4 → F1.5 → F1.6 → F1.7 → F1.8 순차 실행
3. F1.8 완료 시 Wave 1 AC 자동 검증 (`node tests/run-all.js --suite=tc04,tc38,smoke`)
4. PASS 시 do phase `complete` → qa phase 진입 (Trust >= L3 시 자동, L2 시 결재 통합)
5. FAIL 시 `QUALITY_GATE_FAIL` 트리거 → sprint paused

---

## §7. Open Risks (design 단계 미해소)

| Risk | 해소 phase | 사전 대응 |
|---|---|---|
| R-design-1: 21 agent FULL tier 21개 token 추정 부정확 | qa | F2.5 시작 전 5개 agent 사전 spot으로 token 추정 + budget 50K 초과 시 fallback 5개 |
| R-design-2: tc38 매트릭스 추가 9 항목과 기존 tc38 케이스 충돌 | do (F1.6) | F1.6 직후 `node tests/run-all.js --suite=tc38` 즉시 검증 |
| R-design-3: F3.3 SKILL 12 단락 작성 시간 30분 초과 | report (F3) | 단락 7~11 (preview cycle) 그대로 인계, 신규 작성은 단락 12 (v0.42.0 stable)만 |

---

## §8. References

### 8.1 본 sprint 산출물 (4개 완성)

| 산출물 | 경로 | 상태 |
|---|---|---|
| Sprint Master Plan | `docs/01-plan/sprints/v0.42.0-stable-migration-master-plan.md` | ✅ |
| Sprint PRD | `docs/01-plan/sprints/v0.42.0-stable-migration-prd.md` | ✅ |
| Sprint Plan | `docs/01-plan/sprints/v0.42.0-stable-migration-plan.md` | ✅ |
| **Sprint Design (본 문서)** | `docs/01-plan/sprints/v0.42.0-stable-migration-design.md` | ✅ |

### 8.2 입력 산출물

| 산출물 | 경로 | 본 design 인용 §|
|---|---|---|
| P1 Research | `docs/01-plan/research/gemini-cli-v0.42.0-research.md` | §1, §2 (8-stage data flow) |
| P2 Impact Analysis | `docs/03-analysis/gemini-cli-v0.42.0-impact.analysis.md` | §1, §5 |
| P3 Plan | `docs/01-plan/features/gemini-cli-v0.42.0-migration.plan.md` | §2, §3 (Wave 분할 + AC) |
| P4 Report | `docs/04-report/gemini-cli-v0.42.0-migration.report.md` | §4 (M Gate 매트릭스) |

### 8.3 Sprint 표준 (v2.1.13)

- `bkit-system/rules/sprint-management.md` (8-Phase + 4 Auto-Pause + M1~M10)
- `bkit-system/agents/sprint-master-planner.md` (본 agent body)
- `bkit-system/agents/sprint-orchestrator.md` (Phase 진행 관리)
- `bkit-system/agents/sprint-qa-flow.md` (Phase 6 7-Layer dataFlowIntegrity)

---

> **Status**: Draft v1.0 — **M3 PASS** (33 actionable AC, 132% 충족). **L2 stop after design 차단 지점**.
>
> **Next Phase (사용자 결재 후)**: Sprint Phase 4 do → F1 Wave 1 → F2 spot → F4.1~F4.2 → iterate/qa/report/archive.
>
> **결재 항목 (L2 stop)**: D3 (Cx13 채택) + D4 (PR #25827 워크어라운드 v0.42.0 유지) + D5 (PR #25827 정정 흡수 명문화).
