# Sprint v2.0.7-ci-stub-mode — Master Plan

> **Sprint ID**: S1 | **Task ID**: #3 | **Priority**: P3
> **Date**: 2026-05-15 | **Mode**: L4 autonomous

## 1. Mission

L3/L4/L5 manual scenario test 자동화를 위한 **record/replay framework** 구축. `BKIT_TEST_MODE=stub`로 PR-time CI에서 LLM 호출 0회 + 결정성 보장. v2.0.7-debt-resolution master-plan §2 근거.

## 2. Scope

### 2.1 In-scope

- `lib/test/llm-replay.js` — 3-mode 분기 (stub / record / live) + SHA256 hash + fixture I/O
- `tests/fixtures/llm-responses/*.json` 스키마 + sample fixture
- `tests/regression/test-mode/llm-replay.test.js` 동적 회귀 (≥ 8 case)
- `scripts/run-ci-stub.sh` PR-time stub runner placeholder
- `docs/test-plans/v0.42.0-bkit-extension-test-plan.md §11` Stub Mode 절차

### 2.2 Out-of-scope (incremental followup)

- 10건 L3/L4/L5 시나리오 모두 자동화 (framework PoC만)
- GitHub Actions nightly LLM live matrix yaml
- Fixture drift detection (nightly diff alert)

## 3. AC (7)

AC-CS1~CS7 — RCA §4 참조.

## 4. Workflow (Single-day)

| Phase | Output |
|-------|--------|
| Plan | master-plan + plan + design 작성 (RCA 참조) |
| Design | code change list + 동적 testcase 명세 |
| Do | llm-replay.js + sample fixture + 동적 회귀 8+ |
| Check | AC-T6 strict ≥ 1956 무회귀 |
| Report | Final Report |

## 5. Risks

RCA §5 참조. Drift 검출은 별도 sprint로 명시 분리.

## 6. Quality Gates

| Gate | S1 적용 |
|------|---------|
| M1 정합성 | hash 입력 정규화 — 동적 회귀로 강제 |
| M3 보안 | fixture가 LLM 응답에 secret 포함 위험 — captured_at + version만 메타, 사용자 secret 미보관 |
| M5 baseline | AC-T6 strict ≥ 1956 |
| M7 docs sync | test-plans §11 + Sprint 4-doc + Final Report |
| M9 sprint scope | framework + sample fixture + 동적 회귀 — 그 외 변경 X |
