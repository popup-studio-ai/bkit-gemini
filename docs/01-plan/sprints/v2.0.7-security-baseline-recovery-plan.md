# Sprint v2.0.7-security-baseline-recovery — Plan

> **Sprint ID**: S2 | **Date**: 2026-05-15

## 1. Background

- AC-T6 strict baseline 1943/2051. SEC-* 13건 fail (master-plan §3 9건 추정 → 실측 13건).
- Root cause: AGENTS 21-agent 확장 + `_textResponse` helper 리팩토링 + `_sanitizeTeamName` private convention 이후 정적 grep 테스트가 미동기.
- 자세한 RCA: `docs/03-analysis/v2.0.7-security-baseline-rca.md`.

## 2. Goals

1. SUBAGENT_POLICY_GROUPS를 AGENTS source-of-truth로 동기화 (13 readonly + 6 docwrite + 2 full = 21)
2. SEC-* 13건 fail 모두 PASS
3. 4개 동적 보안 회귀 testcase 추가 (정적 grep만 갱신하는 임시방편 X)
4. AC-T6 strict ≥ 1956

## 3. Decisions (D1~D6)

| ID | Decision | Rationale |
|----|----------|-----------|
| D1 | AGENTS = source-of-truth, policy + tests가 그것을 따른다 | mcp/bkit-server.js의 safetyTier는 실제 spawn 시 동작에 영향 |
| D2 | SUBAGENT_POLICY_GROUPS 21-agent 모두 cover | 누락 agent에 정책 없으면 deny default 작동 안 함 위험 |
| D3 | TC80-19/22/24 기대값 truth-update (10→13, 4→6, 34→45) | S3 TC110-01 8→11 패턴 일관성 |
| D4 | TC80-29 cto-lead/pdca-iterator FULL은 그대로 (pm-lead는 DOCWRITE이므로 별도 검사 불요) | full tier는 2명만 유지 |
| D5 | 정적 grep test 갱신 + **동적 회귀 추가** dual | 사용자 원칙 "꼼꼼하고 완벽하게" |
| D6 | 동적 testcase에서 BkitServer.run() 호출 금지 | stdin/stdout hang 방지 |

## 4. Work Breakdown

### Wave 1 (단일 day 진행)

| Step | Output | Cluster |
|------|--------|---------|
| 1 | `lib/gemini/policy.js` SUBAGENT_POLICY_GROUPS 21-agent 동기화 | A |
| 2 | `tests/suites/tc80-security-v200.js` TC80-19/22/24 기대값 + TC80-73 regex escape | A, B |
| 3 | `tests/suites/tc80-security-v200.js` TC80-95 helper-aware 패턴 | C |
| 4 | `tests/suites/tc91-security-v200.js` TC91-40/89/92/94 helper-aware + private convention | C, D |
| 5 | `tests/regression/security/subagent-policy-sync.test.js` 신규 | A 동적 |
| 6 | `tests/regression/security/policy-modes-array.test.js` 신규 | B 동적 |
| 7 | `tests/regression/security/mcp-response-leak.test.js` 신규 | C 동적 |
| 8 | `tests/regression/security/sanitize-team-name.test.js` 신규 | D 동적 |
| 9 | 4 신규 suite + AC-T6 strict 재측정 | 전체 |
| 10 | Final Report | 전체 |

## 5. Acceptance Criteria (master-plan §4 참조)

AC-S1~S6 모두 충족 시 sprint close.

## 6. Risks

- 동적 testcase에서 fs.mkdirSync 실수로 실제 .gemini/teams 디렉터리 오염 → tmpdir 격리 패턴 사용 (S3 BM-12와 동일)
- SUBAGENT_POLICY_GROUPS 동기화 시 generateSubagentRules 출력 길이 증가 → TC80-26/28 회귀 0건 사전 점검
