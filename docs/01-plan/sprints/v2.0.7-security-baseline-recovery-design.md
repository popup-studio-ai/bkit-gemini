# Sprint v2.0.7-security-baseline-recovery — Design

> **Sprint ID**: S2 | **Date**: 2026-05-15

## 1. Code Changes — `lib/gemini/policy.js`

### 1.1 SUBAGENT_POLICY_GROUPS 동기화 (21-agent 전체 cover)

```js
const SUBAGENT_POLICY_GROUPS = Object.freeze({
  readonly: {
    description: 'Read-only agents: analysis, validation, guidance, research',
    agents: [
      // Original (8)
      'gap-detector', 'design-validator', 'code-analyzer',
      'security-architect', 'qa-monitor', 'qa-strategist',
      'starter-guide', 'pipeline-guide',
      // v2.0.5+ PM Agent Team READONLY tier (3)
      'pm-discovery', 'pm-strategy', 'pm-research',
      // v2.0.5+ AGENTS tier-corrected (2): bkend-expert + enterprise-expert
      // (mcp/bkit-server.js:105,111에 READONLY로 등록됨)
      'bkend-expert', 'enterprise-expert'
    ],
    rules: [
      { toolName: 'run_shell_command', decision: 'deny', priority: 100 },
      { toolName: 'write_file', decision: 'deny', priority: 100 },
      { toolName: 'replace', decision: 'deny', priority: 100 }
    ]
  },
  docwrite: {
    description: 'Document-writing agents: reports, specs, design docs, PM orchestration',
    agents: [
      // Original docwrite (4): report-generator, product-manager, infra-architect, frontend-architect
      'report-generator', 'product-manager', 'infra-architect', 'frontend-architect',
      // v2.0.5+ PM Team DOCWRITE tier (2): pm-lead, pm-prd
      'pm-lead', 'pm-prd'
    ],
    rules: [
      { toolName: 'run_shell_command', decision: 'deny', priority: 100 }
    ]
  },
  full: {
    description: 'Full-access agents: implementation, iteration, orchestration',
    agents: [
      'pdca-iterator', 'cto-lead'  // pm-lead 제거 (DOCWRITE로 정정)
    ],
    rules: [
      { toolName: 'run_shell_command', commandPrefix: 'rm -rf /', decision: 'deny', priority: 100 },
      { toolName: 'run_shell_command', commandPrefix: 'git push --force', decision: 'deny', priority: 100 }
    ]
  }
});
```

**총 cover: 13 + 6 + 2 = 21 (AGENTS와 완전 동일)**

## 2. Test Pattern Updates

### 2.1 `tests/suites/tc80-security-v200.js`

| TC | Before | After |
|----|--------|-------|
| TC80-19 | `assertEqual(... readonly.agents.length, 10, ...)` | `assertEqual(... readonly.agents.length, 13, ...)` |
| TC80-22 | `assertEqual(... docwrite.agents.length, 4, ...)` | `assertEqual(... docwrite.agents.length, 6, ...)` |
| TC80-24 | `assertEqual(ruleCount, 34, 'Should generate 34 subagent rules (10*3 + 4*1)')` | `assertEqual(ruleCount, 45, 'Should generate 45 subagent rules (13*3 + 6*1)')` |
| TC80-73 | `/modes\s*=\s*["plan"]/g` (char class bug) | `/modes\s*=\s*\["plan"\]/g` (escaped) |
| TC80-95 | `src.match(/handleGetAgentInfo[\s\S]*?return\s*\{\s*content[...])` (직접 객체 리터럴 가정) | helper-aware: `src.match(/handleGetAgentInfo\(args\)\s*\{[\s\S]*?_textResponse\(\{[\s\S]*?\}\)\s*;\s*\}/)` + `agentContent`/`content:` 미포함 검증 |

### 2.2 `tests/suites/tc91-security-v200.js`

| TC | Before | After |
|----|--------|-------|
| TC91-40 | `src.includes('this.sanitizeTeamName(team_name)')` | `src.includes('this._sanitizeTeamName(team_name)')` |
| TC91-89 | `src.indexOf('handleGetAgentInfo(args)')` (dispatch case 매치) | `src.indexOf('handleGetAgentInfo(args) {')` (정확히 함수 정의 시그니처) |
| TC91-92 | `JSON\.stringify\(\{...\}` (직접 헬퍼 미사용 가정) | `_textResponse\(\{...\}\)` (helper-aware) + agentContent 키 부재 검증 |
| TC91-94 | `/handleListAgents\(\)\s*\{[\s\S]*?return\s*\{[\s\S]*?\};/` (lazy quantifier 다음 함수까지 흡수) | `/handleListAgents\(\)\s*\{[\s\S]*?_textResponse\(\{[^}]*\}\)\s*;\s*\}/` (단일 함수 boundary 보장) |

## 3. New Dynamic Regression — `tests/regression/security/`

### 3.1 `subagent-policy-sync.test.js` (Cluster A)

검증 핵심: AGENTS와 SUBAGENT_POLICY_GROUPS 양방향 정합성을 동적으로 강제. 미래 누가 한 쪽만 수정해도 즉시 fail.

```js
// 1. 모든 21 agent가 정확히 하나의 그룹에 속함
// 2. readonly 그룹의 모든 agent가 AGENTS에서 READONLY tier
// 3. docwrite 그룹의 모든 agent가 AGENTS에서 DOCWRITE tier
// 4. full 그룹의 모든 agent가 AGENTS에서 FULL tier
// 5. AGENTS의 모든 agent가 SUBAGENT_POLICY_GROUPS 어딘가에 등장
```

### 3.2 `policy-modes-array.test.js` (Cluster B)

```js
// 1. Starter / Dynamic / Enterprise 각 policy file이 존재 시:
//    modes 필드는 반드시 [...] 배열 형태 ("plan" 단일 문자열 X)
// 2. plan mode rule이 최소 1건 이상
// 3. plan mode deny priority > normal ask_user priority
```

### 3.3 `mcp-response-leak.test.js` (Cluster C)

```js
// BkitServer 인스턴스 생성 (run() 호출 X)
// 1. handleGetAgentInfo({ agent_name: 'code-analyzer' }) 호출
//    응답 객체를 JSON.stringify → 다음 키워드 미포함 검증:
//      - 'agentContent' (file body)
//      - agents/code-analyzer.md 파일 본문 첫 100 chars
//      - PLUGIN_ROOT 절대경로
//      - '/Users' 또는 'C:\\' 류 OS-absolute prefix
// 2. handleListAgents() 호출 — 동일 검증 + 'extensionPath'/'agentPath' 미포함
// 3. handleGetAgentInfo({ agent_name: 'nonexistent' }) error path — 누설 없음
```

### 3.4 `sanitize-team-name.test.js` (Cluster D)

```js
// BkitServer 인스턴스 생성, tmpdir cwd로 변경 (process.cwd 격리)
// 1. handleTeamCreate({ team_name: 'team;rm -rf /' })
//    → response.success === false, error 메시지 sanitize 거부
//    → fs에 'team;rm*' 파일/디렉터리 생성 안 됨
// 2. handleTeamCreate({ team_name: '../../etc/passwd' })
//    → 동일 거부
// 3. handleTeamCreate({ team_name: 'team@name' })
//    → 동일 거부
// 4. handleTeamCreate({ team_name: 'safe-team_01' })
//    → success
// 5. teardown: tmpdir 정리
```

## 4. Test Runner Pattern (S3 호환)

- 동기 helper + Promise-aware runner (S3 BM integration runner와 동일 패턴)
- tmpdir 격리: `fs.mkdtempSync(os.tmpdir(), 'bkit-s2-...')`
- 각 suite는 단독 `node tests/regression/security/<suite>.test.js`로 실행 가능

## 5. Verification Plan

```
node tests/regression/security/subagent-policy-sync.test.js   # 예상 ALL PASS
node tests/regression/security/policy-modes-array.test.js     # 예상 ALL PASS
node tests/regression/security/mcp-response-leak.test.js      # 예상 ALL PASS
node tests/regression/security/sanitize-team-name.test.js     # 예상 ALL PASS
node tests/run-all.js --strict                                # 목표 ≥ 1956/2051 (+13)
```
